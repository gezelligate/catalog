{
  "name": "gezelligate-catalog",
  "version": "0.1.1",
  "private": true,
  "description": "Versioned catalog of recipes + providers for Gezelligate",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "generate-catalog-json": "tsx scripts/generate-catalog-json.ts",
    "validate": "tsx scripts/validate.ts"
  },
  "devDependencies": {
    "@gezelligate/core": "^0.1.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "js-yaml": "^4.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "zod": "^3.23.8"
  }
}
